(function () {
    "use strict";

    angular
        .module("trackwane")
        .controller("data.tracker-list.tracker-row.controller", controller);

    controller.$inject = ["$scope"];

    function controller($scope) {

        activate();

        function activate() {

        }
    }
})();
